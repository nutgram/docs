import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Handler from "../../src/components/Handler";

# Handlers

## Concept

The framework provides to you a nice API event-like to handling incoming updates:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

$bot->onMessage(function (Nutgram $bot) {
    $bot->sendMessage('You sent a message!');
});

$bot->run();
```

Every `->on*` handler is called based on the update type defined
in [Telegram's update object](https://core.telegram.org/bots/api#update), there are also some specific handlers, which
may respond based on specific patterns or types of messages.

As you can also see from the example above, some required parameters (like the `chat_id`) can be **omitted**, while the
bot is in the context of managing an update, so those fields **are automatically extracted from the current update**.

Of course, **you can override them at any time**, simply by specifying them in the method call.

## Available Handlers

Here a full list of all the handler that listens to specific type of updates:

### Update Handlers

<Handler
    type="generic"
    signature="onUpdate($callable)"
    description="Handles any incoming update."
/>
<Handler
    type="generic"
    signature="onMessage($callable)"
    description="Handles any incoming message."
/>
<Handler
    type="specific"
    signature="onMessageType(string $type, $callable)"
    description="Handles messages defined by type."
/>
<Handler
    type="generic"
    signature="onEditedMessage($callable)"
    description="Handles any incoming edited message."
/>
<Handler
    type="generic"
    signature="onChannelPost($callable)"
    description="Handles any message posted in a channel where the bot is administrator."
/>
<Handler
    type="generic"
    signature="onEditedChannelPost($callable)"
    description="Handles any message edited in a channel where the bot is administrator."
/>
<Handler
    type="generic"
    signature="onBusinessConnection($callable)"
    description="Handles any incoming business connection."
/>
<Handler
    type="generic"
    signature="onEditedBusinessMessage($callable)"
    description="Handles any incoming edited business message."
/>
<Handler
    type="generic"
    signature="onDeletedBusinessMessages($callable)"
    description="Handles any incoming deleted business messages."
/>
<Handler
    type="generic"
    signature="onMessageReaction($callable)"
    description="Handles any incoming message reaction."
/>
<Handler
    type="generic"
    signature="onMessageReactionCount($callable)"
    description="Handles any incoming message reaction count."
/>
<Handler
    type="generic"
    signature="onInlineQuery($callable)"
    description="Handles any incoming inline query."
/>
<Handler
    type="specific"
    signature="onInlineQueryText(string $pattern, $callable)"
    description="Handles any incoming inline query with a specific pattern, similar to `onText`."
/>
<Handler
    type="generic"
    signature="onChosenInlineResult($callable)"
    description="Handles any incoming chosen inline result."
/>
<Handler
    type="specific"
    signature="onChosenInlineResultQuery($callable)"
    description="Handles any incoming chosen inline result with a specific pattern, similar to `onText`."
/>
<Handler
    type="generic"
    signature="onCallbackQuery($callable)"
    description="Handles any incoming callback query."
/>
<Handler
    type="specific"
    signature="onCallbackQueryData(string $pattern, $callable)"
    description="Handles callback query with a specific pattern, similar to `onText`."
/>
<Handler
    type="generic"
    signature="onShippingQuery($callable)"
    description="Handles any incoming shipping query."
/>
<Handler
    type="generic"
    signature="onPreCheckoutQuery($callable)"
    description="Handles any incoming pre checkout query."
/>
<Handler
    type="specific"
    signature="onPreCheckoutQueryPayload(string $pattern, $callable)"
    description="Handles any incoming pre checkout query with a specific payload pattern."
/>
<Handler
    type="generic"
    signature="onPaidMediaPurchased($callable)"
    description="Handles any incoming paid media purchased."
/>
<Handler
    type="specific"
    signature="onPaidMediaPurchasedPayload(string $pattern, $callable)"
    description="Handles any incoming paid media purchased with a specific payload pattern."
/>
<Handler
    type="generic"
    signature="onUpdatePoll($callable)"
    description="Handles any incoming poll."
/>
<Handler
    type="generic"
    signature="onPollAnswer($callable)"
    description="Handles any incoming poll answer."
/>
<Handler
    type="generic"
    signature="onMyChatMember($callable)"
    description="Handles any chat member when updated."
/>
<Handler
    type="generic"
    signature="onChatMember($callable)"
    description="Handles any chat member in other chats when updated."
/>
<Handler
    type="generic"
    signature="onChatJoinRequest($callable)"
    description="Handles any chat join request."
/>
<Handler
    type="specific"
    signature="onChatJoinRequestQuery(string $pattern, $callable)"
    description="Handles any chat join request with a specific query pattern."
/>
<Handler
    type="generic"
    signature="onChatBoost($callable)"
    description="Handles any chat boost."
/>
<Handler
    type="generic"
    signature="onRemovedChatBoost($callable)"
    description="Handles any removed chat boost."
/>
<Handler
    type="generic"
    signature="onManagedBotUpdated($callable)"
    description="Handles any managed bot update."
/>
<Handler
    type="generic"
    signature="onGuestMessage($callable)"
    description="Handles any guest message."
/>
<Handler
    type="generic"
    signature="onBotSubscriptionUpdated($callable)"
    description="Handles any bot subscription update."
/>
<Handler
    type="specific"
    signature="onBotSubscriptionUpdatedPayload(string $pattern, $callable)"
    description="Handles any bot subscription update with a specific payload pattern."
/>

### Message Handlers
<Handler
    type="specific"
    signature="onText(string $pattern, $callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles text messages that match the given pattern (regex or parameters)."
/>
<Handler
    type="specific"
    signature="onCommand(string $command, $callable, UpdateType $target = UpdateType::MESSAGE)"
    description={<>Handles text messages that begin with <code>/</code>.<br/>Automatically parses commands like <code>cmd@botname</code>.</>}
/>
<Handler
    type="generic"
    signature="onRichMessage($callback)"
    description="Handles any rich message."
/>
<Handler
    type="generic"
    signature="onAnimation($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles animation messages"
/>
<Handler
    type="generic"
    signature="onAudio($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles audio messages."
/>
<Handler
    type="generic"
    signature="onDocument($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles document messages."
/>
<Handler
    type="generic"
    signature="onPhoto($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles photo messages."
/>
<Handler
    type="generic"
    signature="onSticker($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles sticker messages."
/>
<Handler
    type="generic"
    signature="onVideo($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles video messages."
/>
<Handler
    type="generic"
    signature="onVideoNote($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles video_note messages."
/>
<Handler
    type="generic"
    signature="onVoice($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles voice messages."
/>
<Handler
    type="generic"
    signature="onContact($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles contact messages."
/>
<Handler
    type="generic"
    signature="onDice($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles dice messages."
/>
<Handler
    type="generic"
    signature="onGame($callable)"
    description="Handles game messages."
/>
<Handler
    type="generic"
    signature="onMessagePoll($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles poll messages."
/>
<Handler
    type="generic"
    signature="onVenue($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles venue messages."
/>
<Handler
    type="generic"
    signature="onLocation($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles location messages."
/>
<Handler
    type="generic"
    signature="onStory($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles story messages."
/>
<Handler
    type="generic"
    signature="onNewChatMembers($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles new_chat_members messages."
/>
<Handler
    type="generic"
    signature="onLeftChatMember($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles left_chat_member messages."
/>
<Handler
    type="generic"
    signature="onChatOwnerLeft($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles chat_owner_left messages."
/>
<Handler
    type="generic"
    signature="onChatOwnerChanged($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles chat_owner_changed messages."
/>
<Handler
    type="generic"
    signature="onNewChatTitle($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles new_chat_title messages."
/>
<Handler
    type="generic"
    signature="onNewChatPhoto($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles new_chat_photo messages."
/>
<Handler
    type="generic"
    signature="onDeleteChatPhoto($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles delete_chat_photo messages."
/>
<Handler
    type="generic"
    signature="onGroupChatCreated($callable)"
    description="Handles group_chat_created messages."
/>
<Handler
    type="generic"
    signature="onSupergroupChatCreated($callable)"
    description="Handles supergroup_chat_created messages."
/>
<Handler
    type="generic"
    signature="onChannelChatCreated($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles channel_chat_created messages."
/>
<Handler
    type="generic"
    signature="onMessageAutoDeleteTimerChanged($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles message_auto_delete_timer_changed messages."
/>
<Handler
    type="generic"
    signature="onMigrateToChatId($callable)"
    description="Handles migrate_to_chat_id messages."
/>
<Handler
    type="generic"
    signature="onMigrateFromChatId($callable)"
    description="Handles migrate_from_chat_id messages."
/>
<Handler
    type="generic"
    signature="onPinnedMessage($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles pinned_message messages."
/>
<Handler
    type="generic"
    signature="onInvoice($callable)"
    description="Handles invoice messages."
/>
<Handler
    type="generic"
    signature="onSuccessfulPayment($callable)"
    description="Handles successful_payment messages."
/>
<Handler
    type="specific"
    signature="onSuccessfulPaymentPayload(string $pattern, $callable)"
    description="Handles successful_payment messages with a specific payload pattern."
/>
<Handler
    type="generic"
    signature="onRefundedPayment($callable)"
    description="Handles refunded_payment messages."
/>
<Handler
    type="specific"
    signature="onRefundedPaymentPayload(string $pattern, $callable)"
    description="Handles refunded_payment messages with a specific payload pattern."
/>
<Handler
    type="generic"
    signature="onUsersShared($callable)"
    description="Handles any users shared."
/>
<Handler
    type="generic"
    signature="onChatShared($callable)"
    description="Handles any chat shared."
/>
<Handler
    type="generic"
    signature="onConnectedWebsite($callable)"
    description="Handles connected_website messages."
/>
<Handler
    type="generic"
    signature="onPassportData($callable)"
    description="Handles any incoming passport data messages."
/>
<Handler
    type="generic"
    signature="onProximityAlertTriggered($callable)"
    description="Handles proximity_alert_triggered messages."
/>
<Handler
    type="generic"
    signature="onBoostAdded($callable)"
    description="Handles boost_added messages."
/>
<Handler
    type="generic"
    signature="onDirectMessagePriceChanged($callable)"
    description="Handles direct_message_price_changed messages."
/>
<Handler
    type="generic"
    signature="onChecklist($callable)"
    description="Handles checklist messages."
/>
<Handler
    type="generic"
    signature="onChecklistTasksDone($callable)"
    description="Handles checklist_tasks_done messages."
/>
<Handler
    type="generic"
    signature="onChecklistTasksAdded($callable)"
    description="Handles checklist_tasks_added messages."
/>
<Handler
    type="generic"
    signature="onForumTopicCreated($callable)"
    description="Handles any forum topic created."
/>
<Handler
    type="generic"
    signature="onForumTopicEdited($callable)"
    description="Handles any forum topic edited."
/>
<Handler
    type="generic"
    signature="onForumTopicClosed($callable)"
    description="Handles any forum topic closed."
/>
<Handler
    type="generic"
    signature="onForumTopicReopened($callable)"
    description="Handles any forum topic reopened."
/>
<Handler
    type="generic"
    signature="onGiveawayCreated($callable)"
    description="Handles giveaway created messages."
/>
<Handler
    type="generic"
    signature="onGiveaway($callable)"
    description="Handles any giveaway."
/>
<Handler
    type="generic"
    signature="onGiveawayWinners($callable)"
    description="Handles giveaway winners messages."
/>
<Handler
    type="generic"
    signature="onGiveawayCompleted($callable)"
    description="Handles giveaway completed messages."
/>
<Handler
    type="generic"
    signature="onVideoChatScheduled($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles video_chat_scheduled messages."
/>
<Handler
    type="generic"
    signature="onVideoChatStarted($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles video_chat_started messages."
/>
<Handler
    type="generic"
    signature="onVideoChatEnded($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles video_chat_ended messages."
/>
<Handler
    type="generic"
    signature="onVideoChatParticipantsInvited($callable, UpdateType $target = UpdateType::MESSAGE)"
    description="Handles video_chat_participants_invited messages."
/>
<Handler
    type="generic"
    signature="onWebAppData($callable)"
    description="Handles web_app_data messages."
/>
<Handler
    type="generic"
    signature="onManagedBotCreated($callable)"
    description="Handles managed_bot_created messages."
/>
<Handler
    type="generic"
    signature="onPollOptionAdded($callable)"
    description="Handles poll_option_added messages."
/>
<Handler
    type="generic"
    signature="onPollOptionDeleted($callable)"
    description="Handles poll_option_deleted messages."
/>
<Handler
    type="generic"
    signature="onCommunityChatAdded($callable)"
    description="Handles community_chat_added messages."
/>
<Handler
    type="generic"
    signature="onCommunityChatRemoved($callable)"
    description="Handles community_chat_removed messages."
/>

### Special Handlers
<Handler
    type="special"
    signature="fallback($callable)"
    description="This handler if defined will be called if no handler, specific or generic, has been found for the current update."
/>
<Handler
    type="special"
    signature="fallbackOn(string $type, $callable)"
    description="This handler has the same behavior as the previous one, but allows you to put a filter on the type of updates it can handle."
/>
<Handler
    type="special"
    signature="onException($callable)"
    description={<>This handler will be called whenever the handling of an update throws an exception, if undefined the exception will not be caught.<br/>Check the next paragraph for more details.</>}
/>
<Handler
    type="special"
    signature="onApiError($callable)"
    description={<>This handler will be called every time a call to Telegram's api fails, if undefined the exception will not be caught.<br/>Check the next paragraph for more details.</>}
/>
<Handler
    type="special"
    signature="beforeApiRequest($callable)"
    description={<>This handler will be called before every call to Telegram's api, if undefined the call will be made without any modification.<br/>Check the next paragraph for more details.</>}
/>
<Handler
    type="special"
    signature="afterApiRequest($callable)"
    description={<>This handler will be called after every call to Telegram's api, if undefined the call will be made without any modification.<br/>Check the next paragraph for more details.</>}
/>

## Specific & Special Handlers

### `onCommand`

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

// Called when a message contains the command "/start someParameter"
$bot->onCommand('start {parameter}', function (Nutgram $bot, $parameter) {
    $bot->sendMessage("The parameter is {$parameter}");
});

// Called on command "/help"
$bot->onCommand('help', function (Nutgram $bot) {
    $bot->sendMessage('Help me!');
});

$bot->run();
```

### `onText`

For text messages, is possible also put parameters to match a regex, or to match part of text:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

// ex. called when a message contains "My name is Mario"
$bot->onText('My name is {name}', function (Nutgram $bot, $name) {
    $bot->sendMessage("Hi {$name}");
});

// ex. called when a message contains "I want 6 pizzas"
$bot->onText('I want ([0-9]+) pizzas', function (Nutgram $bot, $n) {
    $bot->sendMessage("You will get {$n} pizzas!");
});

$bot->run();
```

### `onMessageType`

It's like the `onMessage` handler, but you can specify to which type of message you should handle:

```php
use SergiX44\Nutgram\Nutgram;
use SergiX44\Nutgram\Telegram\Properties\MessageType;

$bot = new Nutgram($_ENV['TOKEN']);

// Called only when you send a photo
$bot->onMessageType(MessageType::PHOTO, function (Nutgram $bot) {
    $photos = $bot->message()->photo;
    $bot->sendMessage('Nice pic!');
});

// Called only when you send an audio file
$bot->onMessageType(MessageType::AUDIO, function (Nutgram $bot) {
    $audio = $bot->message()->audio;
    $bot->sendMessage('I love this song!');
});

$bot->run();
```

You can see all the constants, in
the [MessageType::class](https://github.com/nutgram/nutgram/blob/master/src/Telegram/Properties/MessageType.php).

### `onCallbackQueryData`

It's like the `onText` handler, but you can specify to which `data` contained in CallbackQuery to handle:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

$bot->onCommand('start', function (Nutgram $bot) {
    $bot->sendMessage(
        text: 'Choose an option:',
        reply_markup: InlineKeyboardMarkup::make()->addRow(
            InlineKeyboardButton::make('One', callback_data: 'one'),
            InlineKeyboardButton::make('Two', callback_data: 'two'),
            InlineKeyboardButton::make('Cancel', callback_data: 'cancel'),
        )
    );
});

$bot->onCallbackQueryData('one|two', function (Nutgram $bot) {
    $bot->sendMessage('Nice!');
    $bot->answerCallbackQuery();
});

$bot->onCallbackQueryData('cancel', function (Nutgram $bot) {
    $bot->sendMessage('Canceled!');
    $bot->answerCallbackQuery();
});

$bot->run();
```

The same thing also applies for custom parameters:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

$bot->onCommand('start', function (Nutgram $bot) {
    $bot->sendMessage(
        text: 'Choose an option:',
        reply_markup: InlineKeyboardMarkup::make()->addRow(
            InlineKeyboardButton::make('One', callback_data: 'number 1'),
            InlineKeyboardButton::make('Two', callback_data: 'number 2'),
            InlineKeyboardButton::make('Cancel', callback_data: 'cancel'),
        )
    );
});

$bot->onCallbackQueryData('number {param}', function (Nutgram $bot, $param) {
    $bot->sendMessage($param); // 1 or 2
    $bot->answerCallbackQuery();
});

$bot->onCallbackQueryData('cancel', function (Nutgram $bot) {
    $bot->sendMessage('Canceled!');
    $bot->answerCallbackQuery();
});

$bot->run();
```

### `fallback`

This handler, if defined, will be called every time an `Update` will not match any other defined handler:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

// But the user send something else than /start
$bot->onCommand('start', function (Nutgram $bot) {
    $bot->sendMessage('Started!');
});

$bot->fallback(function (Nutgram $bot) {
    $bot->sendMessage('Sorry, I don\'t understand.');
});

$bot->run();
```

### `fallbackOn`

This has the same behaviour of the `fallback`, but allow you to define handlers based on the `Update` type:

```php
use SergiX44\Nutgram\Nutgram;
use SergiX44\Nutgram\Telegram\Properties\UpdateType;

$bot = new Nutgram($_ENV['TOKEN']);

// define some handlers ...

// Called only for unmatched callback queries
$bot->fallbackOn(UpdateType::CALLBACK_QUERY, function (Nutgram $bot) {
    $bot->answerCallbackQuery();
    $bot->editMessageReplyMarkup([/* ... */]);
});

// Called only for unmatched messages
$bot->fallbackOn(UpdateType::MESSAGE, function (Nutgram $bot) {
    $bot->sendMessage('Sorry, I don\'t understand.');
});

$bot->run();
```

You can see all the constants, in
the [UpdateType::class](https://github.com/nutgram/nutgram/blob/master/src/Telegram/Properties/UpdateType.php).

### `onException`

This handler, if defined, will be called if something on your other handlers goes wrong, passing the `$exception` as
second argument:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

// define some handlers ...

// and exception is thrown...
$bot->onMessage(function (Nutgram $bot) {
    // do stuff
    throw new Exception('Oh no!');
});

// ... and passed to the exception handler
$bot->onException(function (Nutgram $bot, \Throwable $exception) {
    echo $exception->getMessage(); // Oh no!
    $bot->sendMessage('Whoops!');
});

$bot->run();
```

:::tip
For a more specific exception handling, you can visit the [Handling Exceptions](exceptions#onexception-handler-explicit) section.
:::

### `onApiError`

The same concept of the `onException`, but for outgoing requests:

```php
use SergiX44\Nutgram\Nutgram;
use SergiX44\Nutgram\Telegram\Exceptions\TelegramException;

$bot = new Nutgram($_ENV['TOKEN']);

$bot->onMessage(function (Nutgram $bot) {
    $bot->sendMessage(
        text: 'Invalid call!',
        chat_id: null,
    );
});

$bot->onApiError(function (Nutgram $bot, TelegramException $exception) {
    echo $exception->getMessage(); // Bad Request: chat not found
    echo $exception->getCode(); // 400
});

$bot->run();
```

:::tip
For a more specific error handling, you can visit the [Handling Exceptions](exceptions#telegram-api-exceptions) section.
:::

### `beforeApiRequest`

This handler, if defined, will be called before every outgoing request, passing the `$request` as second argument:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

$bot->beforeApiRequest(function (Nutgram $bot, array $request, string $endpoint) {
    // modify the outgoing request changing the text of the message if the endpoint is sendMessage
    if($endpoint === 'sendMessage'){
        $request['json']['text'] = 'Modified!';
    }

    return $request;
});

$bot->run();
```

### `afterApiRequest`

This handler, if defined, will be called after every outgoing request, passing the `$response` as second argument:

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

$bot->afterApiRequest(function (Nutgram $bot, object $response) {
    // print a value from the response
    echo $response->result->text;

    // modify the response
    $response->result->text = 'Modified!';

    return $response;
});

$bot->run();
```

## Handlers Pattern

Nutgram offers a powerful way to handle commands and specific handlers using a pattern system based on regex.<br/>
You can use regex to match specific parts of the text, and you can also use named parameters to extract values from the text.

```php
// called on command "/start"
$bot->onCommand('start', function (Nutgram $bot) {
    $bot->sendMessage('This is the start command!');
});

// called on command "/hello Mario" (named parameter)
$bot->onCommand('hello {name}', function (Nutgram $bot, string $name) {
    $bot->sendMessage("Hi, {$name}!");
});

// called on text "I want 6 portions of cake" (regex)
$bot->onText('I want ([0-9]+) portions of (pizza|cake)', function (Nutgram $bot, string $amount, string $dish) {
    $bot->sendMessage("You will get {$amount} portions of {$dish}!");
});
```

### Case insensitive

By default, the pattern is **case sensitive**, but you can make it case insensitive by calling the `insensitive()` method on the handler:

```php
// called only on text "hello"
$bot->onText('hello', function (Nutgram $bot) {)
    $bot->sendMessage('Hello!');
});

// called on text "Hello", "HELLO", "hElLo" and so on
$bot->onText('hello', function (Nutgram $bot) {)
    $bot->sendMessage('Hello!');
})->insensitive();
```

You can also use the `insensitive()` method in group method:

```php
$bot->group(function () use (Nutgram $bot) {
    $bot->onText('hello', function (Nutgram $bot) {)
        $bot->sendMessage('Hello!');
    });
    $bot->onText('hi', function (Nutgram $bot) {)
        $bot->sendMessage('Hi!');
    });
})->insensitive();
```

### Named parameters constraints
You may constrain the format of your named parameters using the `where` method on a handler.
The `where` method accepts the name of the parameter and a regular expression defining how the parameter should be constrained:

```php
$bot->onCommand('hello {name}', HelloCommand::class)
    ->where('name', '[A-Za-z]+');

$bot->onCommand('hello {name} {age}', HelloCommand::class)
    ->where(['name' => '[A-Za-z]+', 'age' => '[0-9]+']);
```

:::info
By default, parameters are constrained by the default regular expression `.*`.
:::

Here is the list of the other available constraints:

- `whereIn(string $parameter, array $values)`<br/>
The parameter must be contained in the given array.

```php
$bot->onCommand('confirm {answer}', ConfirmCommand::class)
    ->whereIn('answer', ['y','n']);
```

- `whereAlpha(string $parameter)`<br/>
The parameter must be entirely alphabetic characters.

```php
$bot->onCommand('hello {name}', HelloCommand::class)
    ->whereAlpha('name');
```

- `whereNumber(string $parameter)`<br/>
The parameter must be entirely numeric characters.

```php
$bot->onCommand('age {age}', AgeCommand::class)
    ->whereNumber('age');
```

- `whereAlphaNumeric(string $parameter)`<br/>
The parameter must be entirely alpha-numeric characters.

```php
$bot->onCommand('hello {name}', HelloCommand::class)
    ->whereAlphaNumeric('name');
```

You can also use the `where` methods in group method:

```php
$bot->group(function () use (Nutgram $bot) {
    $bot->onCommand('create {name}', CreateCommand::class);
    $bot->onCommand('delete {name}', DeleteCommand::class);
})->whereAlpha('name');
```

### Custom parameters binding

You can also bind custom parameters to the handler using the `bindParameter` method:

```php
$bot = new Nutgram($_ENV['TOKEN']);
$bot->bindParameter('hex', function(Container $container, string $value){
    return Color::fromHex($value);
});

$bot->onCommand('color {hex}', function(Nutgram $bot, Color $color){
    $bot->sendMessage("The color is {$color->name}");
});
```

## Handlers Priority

:::caution

If you declare <img style={{verticalAlign: 'middle'}} src="https://img.shields.io/badge/-SPECIFIC-blue"/> handlers, 
they will block the execution of <img style={{verticalAlign: 'middle'}} src="https://img.shields.io/badge/-GENERIC-brightgreen"/> handlers.<br/>
**Please keep this in mind when using Nutgram.**

:::

Some examples to clarify this:

<Tabs>
<TabItem value="text" label="Text" default>

```php
// Input text: something

// Specific handler: ✅ it will be executed!
$bot->onText('something', SomethingHandler::class);

// Generic handler: ❌ it will not be executed!
$bot->onMessageType(MessageType::TEXT, MessageTypeTextHandler::class);

// Generic handler: ❌ it will not be executed!
$bot->onMessage(MessageHandler::class);
```
    
</TabItem>

<TabItem value="successful_payment" label="SuccessfulPayment">

```php
// Input payload: donation

// Specific handler: ✅ it will be executed!
$bot->onSuccessfulPaymentPayload('donation', DonationHandler::class);

// Generic handler: ❌ it will not be executed!
$bot->onSuccessfulPayment(SuccessfulPaymentHandler::class);
```

</TabItem>

<TabItem value="callback_query" label="CallbackQuery">

```php
// Input payload: mydata

// Specific handler: ✅ it will be executed!
$bot->onCallbackQueryData('mydata', MyDataHandler::class);

// Generic handler: ❌ it will not be executed!
$bot->onCallbackQuery(CallbackQueryHandler::class);
```

</TabItem>

<TabItem value="pre_checkout_query" label="PreCheckoutQuery">

```php
// Input payload: mydata

// Specific handler: ✅ it will be executed!
$bot->onPreCheckoutQueryPayload('mydata', MyDataHandler::class);

// Generic handler: ❌ it will not be executed!
$bot->onPreCheckoutQuery(PreCheckoutQueryHandler::class);
```

</TabItem>

</Tabs>

## Disable handler registration
You can disable the registration of a specific handler, using the `unless` method:

```php
$donationEnabled = false;

$bot->onCommand('donate', DonateConversation::class)->unless($donationEnabled);
$bot->onCommand('start donate', DonateConversation::class)->unless($donationEnabled);
$bot->onPreCheckoutQuery(PreCheckoutQueryHandler::class)->unless($donationEnabled);
$bot->onSuccessfulPayment(SuccessfulPaymentHandler::class)->unless($donationEnabled);

// or

$bot->group(function(){
    $bot->onCommand('donate', DonateConversation::class);
    $bot->onCommand('start donate', DonateConversation::class);
    $bot->onPreCheckoutQuery(PreCheckoutQueryHandler::class);
    $bot->onSuccessfulPayment(SuccessfulPaymentHandler::class);
})->unless($donationEnabled);
```

:::caution
The `unless` method will be executed only when the handler is registered, not when it is executed.<br/>
If you need to stop the execution of a handler at runtime, you can use a [Middleware](middleware.md).
:::


## Automatically register bot commands

The framework can also automatically set the bot commands for you, if you configure the **description** on it:

<Tabs>
<TabItem value="inline" label="Inline" default>

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

// Called on command "/start"
// It's possible to set a description for each command
// this WILL be automatically registered
$bot->onCommand('start', function (Nutgram $bot) {
    return $bot->sendMessage('Hello, world!');
})->description('The start command!');

// Called on command "/secret"
// this WILL NOT be automatically registered
$bot->onCommand('secret', function (Nutgram $bot) {
    return $bot->sendMessage('Shhh');
});
```

</TabItem>

<TabItem value="oop" label="OOP + Interface">

```php
// 1. create a command class like "StartCommand.php":

namespace App\Telegram\Commands;

use SergiX44\Nutgram\Handlers\Type\Command\Command;
use SergiX44\Nutgram\Nutgram;

class StartCommand implements Command
{
    public function description(): string|array
    {
        return 'A lovely start command';
    }

    public function handle(Nutgram $bot): void
    {
        $bot->sendMessage('Hello there!');
    }
}

// 2. Register StartCommand inside Nutgram

$bot->onCommand('start', StartCommand::class);
```

If you want to "hide" a command, simply don't implement the `Command` interface
and don't call the `description` method after the `onCommand` method.

</TabItem>
</Tabs>

### Language code support
Nutgram also supports the ability to set the language code for each command.

To set a command for a specific language, you can use the `description` method with an array of
two-letter ISO 639-1 language code as keys
and the description as value.

You can specify `*` as a wildcard for all languages not explicitly set.


#### No language code
Commands will be applied to all users from the given scope, for whose language there are no dedicated commands.

<Tabs>
<TabItem value="inline" label="Inline" default>

```php
$bot->onCommand('start', StartCommand::class)->description('The start command!');
//or
$bot->onCommand('start', StartCommand::class)->description(['*' => 'The start command!']);
```

</TabItem>

<TabItem value="oop" label="OOP + Interface">

```php
// 1. create a command class like "StartCommand.php":
use SergiX44\Nutgram\Handlers\Type\Command\Command;
use SergiX44\Nutgram\Nutgram;

class StartCommand implements Command
{
    public function description(): string|array
    {
        return 'The start command!';
    }

    public function handle(Nutgram $bot): void
    {
        $bot->sendMessage('Hello there!');
    }
}

// 2. Register StartCommand inside Nutgram
$bot->onCommand('start', StartCommand::class);
```

</TabItem>
</Tabs>

#### Specific language code

<Tabs>
<TabItem value="inline" label="Inline" default>

```php
$bot->onCommand('start', StartCommand::class)->description([
    'it' => 'Il comando start!', // Italian
    'es' => 'El comando start!', // Spanish
    '*' => 'The start command!', // All other languages
]);
```

</TabItem>

<TabItem value="oop" label="OOP + Interface">

```php
// 1. create a command class like "StartCommand.php":
use SergiX44\Nutgram\Handlers\Type\Command\Command;
use SergiX44\Nutgram\Nutgram;

class StartCommand implements Command
{
    public function description(): string|array
    {
        return [
            'it' => 'Il comando start!', // Italian
            'es' => 'El comando start!', // Spanish
            '*' => 'The start command!', // All other languages
        ];
    }

    public function handle(Nutgram $bot): void
    {
        $bot->sendMessage('Hello there!');
    }
}

// 2. Register StartCommand inside Nutgram
$bot->onCommand('start', StartCommand::class);
```

</TabItem>
</Tabs>

### Ephemeral support
You can mark your commands as [ephemeral](https://core.telegram.org/bots/api#ephemeral-messages-and-commands).

<Tabs>
<TabItem value="onCommand-handler" label="onCommand Handler" default>

```php
$bot->onCommand('start', StartCommand::class)
    ->description('The start command')
    ->ephemeral();
```

</TabItem>

<TabItem value="command-class" label="Command Class">

```php
// 1. create a command class like "StartCommand.php":
class StartCommand extends Command
{
    protected string $command = 'start';

    protected ?string $description = 'The start command!';

    protected bool $isEphemeral = true;

    public function handle(Nutgram $bot): void
    {
        $bot->sendMessage('Hello there!');
    }
}

// 2. Register StartCommand inside Nutgram
$bot->registerCommand(StartCommand::class);
```

</TabItem>
</Tabs>

### Scope support
[Command Scopes](https://core.telegram.org/bots/features#command-scopes) allows for more granular control over which users or groups can see specific commands in the bot.

#### How to set a scope

<Tabs>
<TabItem value="inline" label="Inline" default>

Just use the `scope` method:

```php
//single scope
$bot
    ->onCommand('start',  StartCommand::class)
    ->description('Start command')
    ->scope(new BotCommandScopeAllPrivateChats);

// multiple scope (multiple calls)
$bot
    ->onCommand('start',  StartCommand::class)
    ->description('Start command')
    ->scope(new BotCommandScopeAllPrivateChats)
    ->scope(new BotCommandScopeAllGroupChats);

// multiple scope (as array)
$bot
    ->onCommand('start',  StartCommand::class)
    ->description('Start command')
    ->scope([
        new BotCommandScopeAllPrivateChats,
        new BotCommandScopeAllGroupChats,
    ]);
```

</TabItem>

<TabItem value="oop" label="OOP + Interfaces">

To use command scopes you need to implement the `WithScope` interface.

```php
use SergiX44\Nutgram\Handlers\Type\Command\Command;
use SergiX44\Nutgram\Handlers\Type\Command\WithScopes;
use SergiX44\Nutgram\Nutgram;

class StartCommand implements Command, WithScope
{
    public function description(): string|array
    {
        return 'Start command';
    }

    public function scopes(): array
    {
        return [
            new BotCommandScopeAllPrivateChats,
        ];
    }

    public function handle(Nutgram $bot): void
    {
        $bot->sendMessage('Hello there!');
    }
}
```

</TabItem>
</Tabs>

#### Available scopes
- `new BotCommandScopeDefault();`
- `new BotCommandScopeAllChatAdministrators();`
- `new BotCommandScopeAllGroupChats();`
- `new BotCommandScopeAllPrivateChats();`
- `new BotCommandScopeChat(chat_id: 123);`
- `new BotCommandScopeChatAdministrators(chat_id: 123);`
- `new BotCommandScopeChatMember(chat_id: 123, user_id: 987);`

For a description of each scope, please refer to the [official documentation](https://core.telegram.org/bots/api#botcommandscope) page.


### Registering commands

To register the commands, you can use the `registerMyCommands` method, which will automatically call the `setMyCommands` method:

```php
$bot->registerMyCommands();
```
And the result will be:

![commands](https://i.imgur.com/65ofrw7.png)

:::caution

Please do not use the `registerMyCommands` method in the same file where you register your bot handlers when using the [**Webhook**](/docs/usage/getting_updates#webhook) running mode,
because the bot will register your commands on every webhook call, causing a lot of useless requests. Just call the method manually or after a deploy.

:::

## OOP

So far you have seen handlers defined only as closures. But the framework, any definition that accepts a `$callable`, also
accepts a class-method definition, or invokable classes, like this:

```php
use SergiX44\Nutgram\Nutgram;

class MyCommand {

    public function __invoke(Nutgram $bot, $param)
    {
      //do stuff
    }
}
```

```php
use SergiX44\Nutgram\Nutgram;

$bot = new Nutgram($_ENV['TOKEN']);

// all of those are valid definitions:
$bot->onCommand('start {param}', MyCommand::class); // with __invoke
$bot->onCommand('start1 {param}', [MyCommand::class, 'handle']); // class-method
$bot->onCommand('start2 {param}', [$instance, 'handle']); // instance-method

$bot->run();
```
